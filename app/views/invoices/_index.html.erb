<table style="width: 100%;">
	<thead>
		<tr>
			<th class="col-radio"></th>	
			<% current_user.column_names('invoices').each do |column|%>
				<th class="<%=column.column_type%>"><%=t(column.column_name)%></th>	
			<%end%>						
			<th></th>
		</tr>
	</thead>
	<tfoot>
		<tr>
			<th class="list-paginate" colspan="11">
				<%= will_paginate invoices, :param_name => param_name %>			
			</th>
		</tr>
	</tfoot>
	<tbody>
		<% invoices.each do |invoice| %>
		<tr>
			<td>
				<%if choose %>
					<%= fields.radio_button :id, invoice.document_id%>								
				<%end%>
			</td>
			<% current_user.column_names('invoices').each do |column|%>
				<td class="<%=column.column_type%>">
					<%= invoice.try(column.column_query)%>
				</td>	
			<%end%>
			<td class="col-number"><%= link_to "Show", invoice_path(invoice)%></td>
		</tr>
		<% end %>
		<% if invoices.empty? %>
		<tr>
			<td colspan="11">&nbsp;</td>
		</tr>
		<% end %>
	</tbody>
</table>