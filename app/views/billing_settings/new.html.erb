<div class="panel">
	<div class="panel-header">
		<strong><%=t("vat_rate.create")%></strong>
	</div>
	<%= render :partial => "shared/flash", 	:locals => {:flash 		=> flash}%>
	<%= render :partial => "shared/error", 	:locals => {:obj 		=> @vat_rate}%>
	<%= render :partial => "form", 			:locals => {:vat_rate 	=> @vat_rate} %>
</div>
