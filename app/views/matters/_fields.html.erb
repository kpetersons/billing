<%= fields.hidden_field :id %>
<%= fields.hidden_field :parent_id %>
<%= fields.fields_for :matter, @document.matter do |m_f| %>
	<%= m_f.hidden_field :matter_type_id %>
	<%= m_f.hidden_field :operating_party_id %>
	<%= m_f.hidden_field :author_id %>
	<%= m_f.hidden_field :id %>	
<%end%>		
<%= render :partial => "#{@document.matter.matter_type.name.sub('.', '/')}/fields", :locals => {:fields => fields}%>