<div class="panel">
	<div class="panel-header">
		<strong>Edit matter</strong>
	</div>
	<%= render :partial => "shared/flash", 	:locals => {:flash 		=> flash}%>
	<%= render :partial => "shared/error", 	:locals => {:obj 		=> @document}%>	
	<%= render :partial => "form", 			:locals => {:matter 	=> @matter} %>
</div>
