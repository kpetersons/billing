<%=form_for @document, :url => {:controller => 'matters', :action => (@document.persisted?) ?  :update : :create}, :html => { :multipart => true } do |d_f| %>
	<%= render :partial => "fields", 							:locals => {:fields 	=> d_f}%>
	<%= render :partial => "shared/ui/form-submit-controls", 	:locals => {:fields 	=> d_f}%>
<%end%>