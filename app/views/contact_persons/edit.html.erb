<div class="panel">
	<div class="panel-header">
		<strong><%=t("contact_person.edit")%></strong>
	</div>
	<%= render :partial => "shared/flash", 	:locals => {:flash 				=> flash}%>
	<%= render :partial => "shared/error", 	:locals => {:obj 				=> @contact_person}%>	
	<%= render :partial => "form", 			:locals => {:contact_person 	=> @contact_person} %>
</div>
