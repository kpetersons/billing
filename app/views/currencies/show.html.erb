<div class="panel">
	<div class="panel-header">
		<strong><%= t('currency.show')%></strong>
	</div>
	<%= render :partial => "shared/flash", 	:locals => {:flash 		=> flash}%>
	<%= render :partial => "shared/error", 	:locals => {:obj 		=> @currency}%>	
	<%= render :partial => "show", 			:locals => {:currency 	=> @currency} %>
</div>
