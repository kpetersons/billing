<div class="panel">
	<div class="panel-header">
		<strong><%=t('contact.show')%></strong>
	</div>
	<%= render :partial => "shared/flash", :locals => {:flash => flash}%>	
	<%= render :partial => "show" %>	
	<div class="show controls">
		<%= link_to t('button.edit'), {:action => 'edit'} %>
	</div>	
</div>