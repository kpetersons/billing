<div class="panel">
	<div class="panel-header">
		<strong>Create matter task</strong>
	</div>
	<%= render :partial => "shared/flash", 	:locals => {:flash 			=> flash}%>
	<%= render :partial => "shared/error", 	:locals => {:obj 			=> @matter_task}%>
	<%= render :partial => "form", 			:locals => {:matter_task 	=> @matter_task} %>
</div>
